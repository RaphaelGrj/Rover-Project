# Copy this file to pi/rover.env (git-ignored, never commit the real
# one) and fill in your own values. Used by pi/rover-core.service
# (systemd EnvironmentFile) and can also just be `source`d by hand
# before running rover_core directly.

ROVER_CONTROL_TOKEN=replace-with-your-own-long-random-token
